# Example environment variables
# Copy this file to `.env` on the Raspberry and fill with real values (DO NOT commit secrets)

# App environment
APP_ENV=production

# JWT secret used by backend (set a strong random value)
JWT_SECRET=change_me_to_a_secure_random_value

# API prefix
API_PREFIX=/api/v1

# Easyberry settings (used by backend easyberry loader if you prefer env over JSON)
EASYBERRY_URL=https://easyberry-iot.example.com/wp-json/iot-engine/v2
EASYBERRY_USERNAME=your_username
EASYBERRY_PASSWORD=your_password
